function About() {
    return (
        <div className="text">
       <p> GenerateNotes – service for sharing notes. Create a note, send a link to the note and your friend can view it.
                 After viewing, the note will be deleted (or after 15 minutes from the moment it was created).</p>
        </div>
    );
}

export default About;